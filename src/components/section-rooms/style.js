import styled from "styled-components";

const RoomsWrapper = styled.div`
        display: flex;
        flex-wrap: wrap;
        /* 将roomitem最左最右padding消除掉 */
        margin: 0 -8px;
`
export default RoomsWrapper